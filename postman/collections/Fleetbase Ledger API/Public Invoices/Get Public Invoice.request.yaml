$kind: http-request
description: |-
  Retrieves a public invoice. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/public/invoices/{{invoice_public_id}}"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 1000
