$kind: http-request
description: |-
  Returns public invoices matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{api_prefix}}/public/invoices/{{invoice_public_id}}/gateways"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 2000
