$kind: http-request
description: Get Wallet
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/wallet"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  currency: "USD"
order: 1000
