$kind: http-request
description: |-
  Retrieves a wallet. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/wallet/balance"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  currency: "USD"
order: 2000
