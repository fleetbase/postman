$kind: http-request
description: |-
  Deletes a comments resource. The response confirms the resource that was removed.
url: "{{base_url}}/{{namespace}}/comments/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{comment_id}}"

order: 5000
