$kind: http-request
description: |-
  Retrieves a comment. The response includes the current API representation of the resource.
url: "{{base_url}}/{{namespace}}/comments/:id"
method: GET
pathVariables:
  - key: id
    value: "{{comment_id}}"

order: 4000
