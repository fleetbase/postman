$kind: http-request
description: |-
  Returns chat channels matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/chat-channels/available-participants"
method: GET
queryParams:
  channel: "{{chat_channel_id}}"
order: 1000
