$kind: http-request
description: |-
  Updates a chat channels resource. Parameters not supplied remain unchanged.
url: "{{base_url}}/{{namespace}}/chat-channels/:id"
method: PUT
pathVariables:
  - key: id
    value: "{{chat_channel_id}}"

body:
  type: json
  content: |-
    {
      "name": "Dispatch Updates"
    }

order: 6000
