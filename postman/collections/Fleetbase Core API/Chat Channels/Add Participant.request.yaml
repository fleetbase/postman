$kind: http-request
description: |-
  Adds a participant to a chat channel. The response returns the updated channel membership.
url: "{{base_url}}/{{namespace}}/chat-channels/:id/add-participant"
method: POST
pathVariables:
  - key: id
    value: "{{chat_channel_id}}"

body:
  type: json
  content: |-
    {
      "user": "{{user_id}}"
    }

order: 10000
