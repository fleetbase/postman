$kind: http-request
description: |-
  Retrieves a chat channel. The response includes the current API representation of the resource.
url: "{{base_url}}/{{namespace}}/chat-channels/:id"
method: GET
pathVariables:
  - key: id
    value: "{{chat_channel_id}}"

order: 8000
