$kind: params
fields:
  - name: name
    type: string
    required: true
    description: "Name of the chat channel."
  - name: participants
    type: array of strings
    description: "Public user IDs to add as participants after the channel is created."
