$kind: http-request
description: |-
  Deletes a chat channels resource. The response confirms the resource that was removed.
url: "{{base_url}}/{{namespace}}/chat-channels/remove-participant/:participantId"
method: DELETE
pathVariables:
  - key: participantId
    value: "{{chat_participant_id}}"

order: 11000
