$kind: http-request
description: |-
  Removes a participant from a chat channel by participant ID. The channel remains active for the remaining participants.
url: "{{base_url}}/{{namespace}}/chat-channels/remove-participant/:participantId"
method: DELETE
pathVariables:
headers:
  - key: participantId
    value: "{{chat_participant_id}}"

order: 11000
