$kind: http-request
description: |-
  Deletes a chat channel by ID. The response returns a deleted-resource envelope for the removed channel.
url: "{{base_url}}/{{namespace}}/chat-channels/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{chat_channel_id}}"

order: 9000
