$kind: http-request
description: |-
  Uploads a file from base64-encoded content. The response returns the created file resource.
url: "{{base_url}}/{{namespace}}/files/base64"
method: POST
body:
  type: json
  content: |-
    {
      "data": "{{base64_file_data}}",
      "file_name": "example.png",
      "file_type": "image",
      "content_type": "image/png",
      "path": "uploads"
    }

order: 2000
