$kind: http-request
description: |-
  Retrieves a file. The response includes the current API representation of the resource.
url: "{{base_url}}/{{namespace}}/files/:id"
method: GET
pathVariables:
  - key: id
    value: "{{file_id}}"

order: 6000
