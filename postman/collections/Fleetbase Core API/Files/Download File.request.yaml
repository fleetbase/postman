$kind: http-request
description: |-
  Download File for files. The response returns the result of the operation.
url: "{{base_url}}/{{namespace}}/files/:id/download"
method: GET
pathVariables:
  - key: id
    value: "{{file_id}}"

order: 4000
