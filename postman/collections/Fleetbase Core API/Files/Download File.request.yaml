$kind: http-request
description: |-
  Downloads a file by ID. The response returns the file payload or download metadata.
url: "{{base_url}}/{{namespace}}/files/:id/download"
method: GET
pathVariables:
  - key: id
    value: "{{file_id}}"

order: 4000
