$kind: http-request
description: |-
  Deletes a files resource. The response confirms the resource that was removed.
url: "{{base_url}}/{{namespace}}/files/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{file_id}}"

order: 7000
