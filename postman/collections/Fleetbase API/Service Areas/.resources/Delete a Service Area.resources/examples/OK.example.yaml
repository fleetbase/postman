$kind: http-example
request:
  url: "{{base_url}}/{{namespace}}/service-areas/:id"
  method: DELETE
  headers:
    Accept: application/json
  pathVariables:
    id: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: "{}"
order: 1000
