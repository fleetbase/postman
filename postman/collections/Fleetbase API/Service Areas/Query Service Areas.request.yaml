$kind: http-request
description: |-
  Returns service areas matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/service-areas"
method: GET
queryParams:
  name: "{{service_area_name}}"
order: 3000
