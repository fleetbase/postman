$kind: http-request
description: |-
  Retrieves a fuel report. The response includes the current API representation of the resource.
url: "{{base_url}}/{{namespace}}/fuel-reports/:id"
method: GET
pathVariables:
  - key: id
    value: "{{fuel_report_id}}"

order: 3000
