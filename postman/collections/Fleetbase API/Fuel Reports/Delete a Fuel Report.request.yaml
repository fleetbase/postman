$kind: http-request
description: |-
  Deletes a fuel reports resource. The response confirms the resource that was removed.
url: "{{base_url}}/{{namespace}}/fuel-reports/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{fuel_report_id}}"

order: 5000
