$kind: http-request
description: |-
  Returns fuel reports matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/fuel-reports"
method: GET
queryParams:
  limit: "25"
  offset: "0"
  sort: "created_at"
order: 2000
