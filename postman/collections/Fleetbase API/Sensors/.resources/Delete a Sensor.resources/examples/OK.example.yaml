$kind: http-example
request:
  url: "{{base_url}}/{{namespace}}/sensors/{{sensor_id}}"
  method: DELETE
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
              "id": "<string>",
              "deleted": true
            }
