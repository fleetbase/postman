$kind: http-request
description: |-
  Returns a paginated list of fleets for the current organization. Use pagination and sorting parameters to control the result set.
url: "{{base_url}}/{{namespace}}/fleets"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Query Fleets.resources/examples
queryParams:
  limit: "25"
  offset: "0"
  sort: "created_at"
order: 2000
