$kind: http-request
description: |-
  Returns vendors matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/vendors"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Query Vendors.resources/examples
queryParams:
  id: "{{vendor_id}}"
order: 4000
