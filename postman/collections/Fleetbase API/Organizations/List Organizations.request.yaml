$kind: http-request
description: "Lists organizations available for driver onboarding and organization selection."
url: "{{base_url}}/{{namespace}}/organizations?limit=10&with_driver_onboard=false"
method: GET
queryParams:
  - key: limit
    value: "10"
  - key: with_driver_onboard
    value: "false"

order: 1000
