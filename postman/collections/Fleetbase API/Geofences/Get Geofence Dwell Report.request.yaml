$kind: http-request
description: "Get Geofence Dwell Report"
url: "{{base_url}}/{{namespace}}/geofences/dwell-report?from={{from_datetime}}&to={{to_datetime}}"
method: GET
queryParams:
  - key: from
    value: "{{from_datetime}}"
  - key: to
    value: "{{to_datetime}}"

order: 3000
