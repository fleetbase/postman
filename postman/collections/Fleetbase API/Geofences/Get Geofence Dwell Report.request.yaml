$kind: http-request
description: |-
  Retrieves a geofence. The response includes the current API representation of the resource.
url: "{{base_url}}/{{namespace}}/geofences/dwell-report"
method: GET

queryParams:
  from: "{{from_datetime}}"
  to: "{{to_datetime}}"
order: 3000
