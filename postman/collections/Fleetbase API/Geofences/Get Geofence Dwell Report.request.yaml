$kind: http-request
description: "Get Geofence Dwell Report"
url: "{{base_url}}/{{namespace}}/geofences/dwell-report"
method: GET

queryParams:
  from: "{{from_datetime}}"
  to: "{{to_datetime}}"
order: 3000
