$kind: http-request
description: "Get Driver Geofence History"
url: "{{base_url}}/{{namespace}}/geofences/driver/:driverUuid/history?per_page=50"
method: GET
queryParams:
  - key: per_page
    value: "50"

pathVariables:
  - key: driverUuid
    value: "{{driver_uuid}}"

order: 4000
