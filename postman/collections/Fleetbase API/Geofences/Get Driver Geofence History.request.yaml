$kind: http-request
description: "Get Driver Geofence History"
url: "{{base_url}}/{{namespace}}/geofences/driver/:driverUuid/history"
method: GET

pathVariables:
  - key: driverUuid
    value: "{{driver_uuid}}"

queryParams:
  per_page: "50"
order: 4000
