$kind: http-request
description: |-
  Returns geofences matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/geofences/events"
method: GET

queryParams:
  per_page: "50"
  event_type: "entered"
order: 1000
