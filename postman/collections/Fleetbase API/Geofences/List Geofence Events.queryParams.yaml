$kind: params
fields:
  - name: per_page
    type: integer
    description: Number of geofence event records to return per page.
  - name: event_type
    type: string
    description: Filters geofence events by event type.
