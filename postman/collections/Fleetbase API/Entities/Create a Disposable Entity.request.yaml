$kind: http-request
name: "Create a Disposable Entity"
description: |-
  Creates a throwaway entity purely so `Delete a Entity` has
  something to delete. Without it the delete destroys `{{entity_id}}`, which
  later requests in this and other folders still depend on — leaving them to
  404 against a deleted record.
url: "{{base_url}}/{{namespace}}/entities"
method: POST
headers:
  Accept: application/json
body:
  type: json
  content: |-
    {
        "name": "SampleEntity",
        "type": "parcel",
        "payload": "{{payload_id}}",
        "customer": "ACustomer",
        "internal_id": "ENTITY001",
        "description": "Sample description",
        "meta": {
            "warehouse_bin": "1",
            "warehouse_rack": "3",
            "warehouse_section": "4"
        },
        "weight": 2.5,
        "weight_unit": "kg",
        "length": 10,
        "width": 5,
        "height": 8,
        "dimensions_unit": "mm",
        "declared_value": 1500,
        "price": 1200,
        "sale_price": 900,
        "sku": "SKU123",
        "currency": "USD"
    }
scripts:
  - type: afterResponse
    code: |
      var json_response = pm.response.json();
      let cv = pm.environment;
      cv.set("entity_id_disposable", json_response.id);
    language: text/javascript
order: 4999
