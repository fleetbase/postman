$kind: http-example
request:
  url: "{{base_url}}/{{namespace}}/orders/:id/cancel"
  method: DELETE
  headers:
    Accept: application/json
  pathVariables:
    id: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "id": "order_abc123",
        "object": "order",
        "deleted": true
      }
order: 1000
