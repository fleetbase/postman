$kind: http-request
description: |-
  Starts an order and moves it into active fulfillment. Use this when a driver or dispatcher begins work on the order.
url: "{{base_url}}/{{namespace}}/orders/:id/start"
method: POST
pathVariables:
  - key: id
    value: "{{order_id}}"
    description: (Required) The id of the order to dispatch.
body:
  type: json
  content: |-
    {
        "skip_dispatch": false
    }
examples: ./.resources/Start an Order.resources/examples
order: 5000
