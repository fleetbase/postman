$kind: http-request
description: |-
  Returns orders matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/orders"
method: GET
queryParams:
  limit: "25"
  offset: "0"
  sort: "created_at"
  status: "active"
order: 2000
