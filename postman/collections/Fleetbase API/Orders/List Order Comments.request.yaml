$kind: http-request
name: "List Order Comments"
description: "Lists comments attached to an order."
url: "{{base_url}}/{{namespace}}/orders/:id/comments"
method: GET
pathVariables:
  - key: id
    value: "{{order_id}}"

order: 14400
