$kind: http-request
description: Update an Order.
url: "{{base_url}}/{{namespace}}/orders/:id"
method: PUT
pathVariables:
  - key: id
    value: "{{order_id}}"
    description: (Required) The id of the `Order`.
body:
  type: json
  content: |
    {
      "service_quote": "quote_hZ3C21KVkx"
    }
examples: ./.resources/Update an Order.resources/examples
order: 12000
