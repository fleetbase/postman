$kind: http-request
name: "Get Order ETA"
description: "Returns ETA data for an order."
url: "{{base_url}}/{{namespace}}/orders/:id/eta"
method: GET
pathVariables:
  - key: id
    value: "{{order_id}}"

order: 14300
