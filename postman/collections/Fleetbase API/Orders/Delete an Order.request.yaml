$kind: http-request
description: "Deletes an order resource."
url: "{{base_url}}/{{namespace}}/orders/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{order_id}}"
    description: "The order id."

order: 13500
