$kind: http-request
description: |-
  Returns drivers matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/drivers"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Query Drivers.resources/examples
queryParams:
  id: "{{driver_id}}"
order: 2000
