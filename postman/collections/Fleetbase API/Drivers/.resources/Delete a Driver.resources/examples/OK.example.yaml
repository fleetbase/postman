$kind: http-example
request:
  url: "{{base_url}}/{{namespace}}/drivers/:id"
  method: DELETE
  headers:
    Accept: application/json
  pathVariables:
    id: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "deleted": "<boolean>",
        "id": "<string>"
      }
order: 1000
