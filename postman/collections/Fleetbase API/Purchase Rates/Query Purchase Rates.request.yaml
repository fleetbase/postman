$kind: http-request
description: |-
  Returns purchase rates matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/purchase-rates"
method: GET
queryParams:
  limit: "25"
  offset: "0"
  sort: "created_at"
order: 3000
