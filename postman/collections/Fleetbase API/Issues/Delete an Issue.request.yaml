$kind: http-request
description: |-
  Deletes an issues resource. The response confirms the resource that was removed.
url: "{{base_url}}/{{namespace}}/issues/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{issue_id}}"

order: 5000
