$kind: http-request
description: "Update an Issue"
url: "{{base_url}}/{{namespace}}/issues/:id"
method: PUT
pathVariables:
  - key: id
    value: "{{issue_id}}"

body:
  type: json
  content: |-
    {
      "report": "Updated issue report",
      "category": "vehicle",
      "type": "maintenance",
      "priority": "high",
      "status": "resolved"
    }

order: 4000
