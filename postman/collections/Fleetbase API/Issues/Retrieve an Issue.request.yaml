$kind: http-request
description: |-
  Retrieves a issue. The response includes the current API representation of the resource.
url: "{{base_url}}/{{namespace}}/issues/:id"
method: GET
pathVariables:
  - key: id
    value: "{{issue_id}}"

order: 3000
