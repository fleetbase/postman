$kind: http-request
description: Delete a Tracking Status.
url: "{{base_url}}/{{namespace}}/tracking-statuses/:id"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  - key: id
    value: "{{tracking_status_id}}"
    description: (Required) The Tracking Status id.
examples: ./.resources/Delete a Tracking Status.resources/examples
order: 5000
