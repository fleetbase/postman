$kind: http-request
description: |-
  Updates a tracking statuses resource. Parameters not supplied remain unchanged.
url: "{{base_url}}/{{namespace}}/tracking-statuses/:id"
method: PUT
headers:
  Accept: application/json
pathVariables:
  id: "{{tracking_status_id}}"
body:
  type: json
  content: |-
    {
        "country": "SG"
    }
examples: ./.resources/Update a Tracking Status.resources/examples
order: 2000
