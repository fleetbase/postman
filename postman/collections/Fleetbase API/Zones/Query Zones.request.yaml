$kind: http-request
description: This endpoint allows you to retrieve a zone object to view it's details.
url: "{{base_url}}/{{namespace}}/zones"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Query Zones.resources/examples
queryParams:
  name: "{{zone_name}}"
order: 3000
