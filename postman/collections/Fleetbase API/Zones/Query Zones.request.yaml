$kind: http-request
description: |-
  Returns zones matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{namespace}}/zones"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Query Zones.resources/examples
queryParams:
  name: "{{zone_name}}"
order: 3000
