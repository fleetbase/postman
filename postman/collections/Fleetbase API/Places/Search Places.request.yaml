$kind: http-request
description: "Searches places by free-form query and optional lat/lng locale context."
url: "{{base_url}}/{{namespace}}/places/search"
method: GET

queryParams:
  query: "{{place_query}}"
  ll: "{{place_ll}}"
  locale: "{{locale}}"
order: 2500
