$kind: http-example
request:
  url: "{{base_url}}/{{namespace}}/devices/{{device_id}}"
  method: PUT
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
              "id": "<string>",
              "object": "devices"
            }
