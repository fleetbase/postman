$kind: params
fields:
  - name: identity
    type: string
    required: true
    description: Email or phone the reset code was sent to.
  - name: code
    type: string
    required: true
    description: Reset verification code.
  - name: password
    type: string
    required: true
    description: New password (minimum 8 characters).
