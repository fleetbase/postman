$kind: http-request
description: |-
  Returns payloads for the current company. Use pagination and sort parameters to page through payload records.
url: "{{base_url}}/{{namespace}}/payloads"
method: GET
queryParams:
  limit: "25"
  offset: "0"
  sort: "created_at"
order: 2500
