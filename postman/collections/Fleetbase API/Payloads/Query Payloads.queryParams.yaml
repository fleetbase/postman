$kind: params
fields:
  - name: limit
    type: integer
    description: Maximum number of payloads to return.
  - name: offset
    type: integer
    description: Number of payloads to skip before returning results.
  - name: sort
    type: string
    description: Sort expression for the payload query.
