$kind: http-request
description: Retrieve a purchase order
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/purchase-orders/{{purchase_order_id}}"
method: GET
headers:
  - key: Accept
    value: application/json
order: 1200
