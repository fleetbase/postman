$kind: http-request
description: Ship, deducting from the source
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/stock-transfers/{{stock_transfer_id}}/ship"
method: POST
headers:
  - key: Accept
    value: application/json
order: 1400
