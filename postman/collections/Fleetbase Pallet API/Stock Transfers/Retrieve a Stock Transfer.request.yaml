$kind: http-request
description: Retrieve a transfer
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/stock-transfers/{{stock_transfer_id}}"
method: GET
headers:
  - key: Accept
    value: application/json
order: 1200
