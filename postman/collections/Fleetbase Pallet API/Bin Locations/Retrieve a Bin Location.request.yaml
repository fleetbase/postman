$kind: http-request
description: Retrieve a bin location
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/bin-locations/{{bin_id}}"
method: GET
headers:
  - key: Accept
    value: application/json
order: 1200
