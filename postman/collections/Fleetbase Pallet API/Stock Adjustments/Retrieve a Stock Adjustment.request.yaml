$kind: http-request
description: Retrieve an adjustment
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/stock-adjustments/{{stock_adjustment_id}}"
method: GET
headers:
  - key: Accept
    value: application/json
order: 1200
