$kind: params
fields:
  - name: ll
    type: string
    description: "Latitude and longitude used as the search origin."
  - name: locale
    type: string
    description: "Locale used to format place search results."
  - name: query
    type: string
    description: "Search text for the place lookup."
