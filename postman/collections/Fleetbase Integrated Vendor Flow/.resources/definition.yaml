$kind: collection
description: |-
  This workflow shows how to quote, create, inspect, and cancel an order that uses an integrated vendor. Follow the steps in order to move from place lookup through service quote selection and order creation.
variables:
  base_url: ""
  namespace: v1
  api_key: ""
auth:
  - id: ccab1f0e-177f-4083-92de-0609f8154be1
    type: bearer
    name: bearer auth
    credentials:
      token: "{{api_key}}"
