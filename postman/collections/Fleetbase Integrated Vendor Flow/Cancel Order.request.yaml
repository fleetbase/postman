$kind: http-request
description: |-
  Cancels the order created from the integrated vendor service quote workflow.
url: "{{base_url}}/{{namespace}}/orders/{{order_id}}/cancel"
method: DELETE
order: 7000
