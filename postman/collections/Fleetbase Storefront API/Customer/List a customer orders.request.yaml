$kind: http-request
description: |-
  Returns customer matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/customers/orders"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 10000
