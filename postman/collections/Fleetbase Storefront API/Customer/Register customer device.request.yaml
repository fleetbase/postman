$kind: http-request
description: |-
  Registers a push notification device token for the authenticated storefront customer. Use this endpoint to enable customer device notifications.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/customers/register-device"
method: POST
headers:
  - key: customer-token
    value: ""
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: ""
order: 5000
