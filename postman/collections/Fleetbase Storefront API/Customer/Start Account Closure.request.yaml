$kind: http-request
description: |-
  Starts account closure for the authenticated storefront customer. Fleetbase sends a confirmation code before the account can be closed.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/customers/account-closure"
method: POST
headers:
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {}
order: 8000
