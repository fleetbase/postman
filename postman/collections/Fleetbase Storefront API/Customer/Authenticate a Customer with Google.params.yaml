$kind: params
fields:
  - name: idToken
    type: string
    required: true
    description: "Google ID token to verify."
  - name: clientId
    type: string
    required: true
    description: "Google OAuth client ID expected for the ID token."
