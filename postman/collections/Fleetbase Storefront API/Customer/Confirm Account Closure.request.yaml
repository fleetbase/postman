$kind: http-request
description: |-
  Confirms account closure with the verification code sent to the customer. The response returns the closure result.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/customers/confirm-account-closure"
method: POST
headers:
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "code": "{{verification_code}}"
    }
order: 8100
