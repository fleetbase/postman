$kind: http-request
description: |-
  Verifies a storefront SMS or account-creation code. The response returns the customer profile and access token when verification succeeds.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/customers/verify-code"
method: POST
headers:
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: ""
order: 3000
