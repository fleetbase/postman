$kind: http-request
description: |-
  Authenticates a storefront customer with an email or phone identity and password. The response returns the customer profile and access token.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/customers/login"
method: POST
headers:
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: ""
order: 1000
