$kind: http-request
description: |-
  Returns saved places for the authenticated storefront customer. Use these places for delivery addresses and checkout defaults.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/customers/places"
method: GET
order: 9000
