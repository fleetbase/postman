$kind: http-request
description: |-
  Creates a checkout resource. The response returns the created resource with its generated identifier.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/checkouts/stripe-setup-intent"
method: POST
headers:
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "customer": "{{customer_id}}"
    }
order: 6000
