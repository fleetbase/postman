$kind: http-request
description: |-
  Retrieves a checkout. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/checkouts/status"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  checkout: "{{checkout_id}}"
  token: "{{checkout_token}}"
order: 5000
