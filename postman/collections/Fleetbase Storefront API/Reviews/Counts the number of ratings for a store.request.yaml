$kind: http-request
description: |-
  Returns the number of storefront ratings for the current store. Use it to display aggregate review counts without fetching the full review list.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/reviews/count"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 3000
