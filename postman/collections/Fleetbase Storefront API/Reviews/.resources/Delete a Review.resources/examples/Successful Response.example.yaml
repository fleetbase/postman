$kind: http-example
request:
  url: "{{base_url}}/{{api_prefix}}/{{namespace}}/reviews/{id}"
  method: DELETE
  headers:
    Accept: application/json
  pathVariables:
    id: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "id": "review_abc123",
        "object": "review",
        "deleted": true
      }
order: 1000
