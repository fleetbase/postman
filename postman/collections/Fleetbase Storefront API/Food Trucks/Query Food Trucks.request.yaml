$kind: http-request
description: |-
  Returns food trucks matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/food-trucks"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  limit: ""
  offset: ""
  sort: ""
order: 1000
