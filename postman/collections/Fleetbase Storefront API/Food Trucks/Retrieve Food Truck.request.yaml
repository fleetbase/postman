$kind: http-request
description: |-
  Retrieves a food truck. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/food-trucks/{{food_truck_id}}"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 2000
