$kind: http-request
description: |-
  Marks a pickup order as completed by the authenticated customer. The response returns the updated order pickup status.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/orders/picked-up"
method: PUT
headers:
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "order": "{{order_id}}"
    }
order: 1000
