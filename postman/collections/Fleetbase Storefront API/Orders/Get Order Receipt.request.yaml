$kind: http-request
description: |-
  Retrieves a order. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/orders/receipt"
method: POST
headers:
  - key: Authorization
    value: ""
    description: Authentication header
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "order": "{{order_id}}",
      "ebarimt_receiver_type": "CITIZEN",
      "ebarimt_receiver": ""
    }
order: 2000
