$kind: http-request
description: |-
  Returns products matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/products"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  offset: ""
order: 1000
