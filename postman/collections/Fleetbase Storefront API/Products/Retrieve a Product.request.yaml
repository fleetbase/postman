$kind: http-request
description: |-
  Retrieves a product. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/products/{id}"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
pathVariables:
  id: ""
order: 2000
