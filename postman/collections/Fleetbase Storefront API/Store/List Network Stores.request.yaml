$kind: http-request
description: |-
  Returns store matching the supplied filters. Use pagination and query parameters to control the result set.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/stores"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  query: ""
  limit: ""
  offset: ""
  store: ""
order: 2700
