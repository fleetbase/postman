$kind: http-request
description: |-
  Searches the current storefront catalog and related store data. Use query parameters to narrow the results returned to the shopper.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/search"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  query: ""
  limit: ""
  offset: ""
order: 1000
