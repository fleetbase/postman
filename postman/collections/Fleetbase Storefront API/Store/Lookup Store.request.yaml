$kind: http-request
description: Lookup Store
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/lookup/{{store_id}}"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 2100
