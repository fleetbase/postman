$kind: http-request
description: |-
  Retrieves a payment gateway by ID for the current storefront. The response includes the gateway metadata safe for storefront clients.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/gateways/{id}"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
pathVariables:
  id: ""
order: 4000
