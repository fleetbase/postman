$kind: http-request
description: |-
  Specific payment gateway given the id for store. The response returns the result of the operation.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/gateways/{id}"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
pathVariables:
  id: ""
order: 4000
