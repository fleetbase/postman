$kind: http-request
description: |-
  Returns public profile and configuration details for the current storefront. Use this endpoint to initialize storefront clients.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/about"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 2000
