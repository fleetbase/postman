$kind: http-request
description: |-
  Payment gateways for the current store for store. The response returns the result of the operation.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/gateways"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  limit: ""
  offset: ""
order: 3000
