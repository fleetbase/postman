$kind: http-request
description: |-
  Lists payment gateways available to the current storefront. Use the response to decide which payment methods can be offered at checkout.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/gateways"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  limit: ""
  offset: ""
order: 3000
