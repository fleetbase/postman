$kind: http-request
description: |-
  Retrieves a delivery service quote. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/service-quotes/from-cart"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
queryParams:
  origin: ""
  destination: ""
  cart: ""
order: 1000
