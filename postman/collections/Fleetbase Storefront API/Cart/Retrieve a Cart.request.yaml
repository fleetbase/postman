$kind: http-request
description: |-
  Retrieves a cart. The response includes the current API representation of the resource.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/carts/{id}"
method: GET
headers:
  - key: Authorization
    value: ""
    description: Authentication header
pathVariables:
  id: ""
order: 2000
