$kind: http-request
description: |-
  Deletes a cart resource. The response confirms the resource that was removed.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/carts/{{cart_id}}"
method: DELETE
headers:
  - key: Authorization
    value: ""
    description: Authentication header
order: 6000
