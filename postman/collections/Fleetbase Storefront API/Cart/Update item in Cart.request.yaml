$kind: http-request
description: |-
  Updates a cart resource. Parameters not supplied remain unchanged.
  You can update all properties of the Zone.

url: "{{base_url}}/{{api_prefix}}/{{namespace}}/carts/{id}/{line_item_id}"
method: PUT
headers:
  - key: Authorization
    value: ""
    description: Authentication header
pathVariables:
  id: ""
  line_item_id: ""
order: 3000
