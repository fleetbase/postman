$kind: http-request
description: |-
  Empty cart for cart. The response returns the result of the operation.
url: "{{base_url}}/{{api_prefix}}/{{namespace}}/carts/{id}/empty"
method: PUT
headers:
  - key: Authorization
    value: ""
    description: Authentication header
pathVariables:
  id: ""
order: 5000
